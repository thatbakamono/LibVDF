<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Authors>KernelError</Authors>
    <PackageTags>valve, data, file, library</PackageTags>
    <RepositoryUrl>https://github.com/KernelErr0r/LibVDF</RepositoryUrl>
    <PackageProjectUrl>https://github.com/KernelErr0r/LibVDF</PackageProjectUrl>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Company />
    <Description>A library to interact with valve data files (VDF)</Description>
  </PropertyGroup>

</Project>
